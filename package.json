{
  "name": "begambeli-radio",
  "private": true,
  "type": "module",
  "description": "Not a build step. This file exists so Node treats assets/js/*.js as ES modules, which is what lets scripts/test-schedule.mjs import the scheduling code the browser runs. There are no dependencies and nothing to install. Cloudflare Pages must be configured with no build command.",
  "scripts": {
    "test": "node scripts/test-schedule.mjs",
    "tracks": "node scripts/build-tracks.mjs",
    "serve": "python3 -m http.server 8788"
  }
}
